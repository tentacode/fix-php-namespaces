#!/usr/bin/env php
<?php

require_once __DIR__ . '/../vendor/autoload.php';

$app = new Silly\Application();

$app->command('run', new Tentacode\FixNamespaceCommand);
$app->setDefaultCommand('run');

$app->run();
